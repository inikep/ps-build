- job:
    name: percona-server-8.0-pipeline-testing-inikep3
    project-type: pipeline
    description: |
        <b>Do not make changes to the pipeline definition via the web interface!<br>
        (Do not use 'Configure' option on the left for changing the pipeline definition)<br>
        Change the YAML files and Groovy scripts in the <a href="https://github.com/inikep/ps-build/tree/PS-8979-pipeline-testing/jenkins">git repo</a> instead.</b>
    concurrent: true
    pipeline-scm:
      scm:
        - git:
            url: https://github.com/inikep/ps-build
            branches:
            - "PS-8979-pipeline-testing3"
            wipe-workspace: false
      lightweight-checkout: true
      script-path: jenkins/pipeline-testing.groovy
